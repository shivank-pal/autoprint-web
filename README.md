# AutoPrint Web

Run with:

```powershell
npm install
npm run dev
```

Then open http://localhost:3000.

The current version is a runnable frontend foundation. Supabase is intentionally optional until real values are added to `.env.local`.
